theta = zeros(1, 6);
d = [0.1625, 0, 0, 0.1333, 0.0997, 0.0996];
a = [0, -0.425, -0.3922, 0, 0, 0];
alpha = [pi / 2, 0, 0, pi / 2, -pi / 2, 0];

links = [];
for i = 1 : 6
    links(i) = Link('revolute', 'd', d(i), 'a', a(i), 'alpha', alpha(i), 'offset', 0);
end

robot = SerialLink(links, 'name', 'UR5e');

posA = deg2[ -90, -173, 132, 220, 0, 0]